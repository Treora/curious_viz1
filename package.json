{
  "name": "curious_viz",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "browserify src/main.js -o main.js",
    "watch": "watchify -v src/main.js -o main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gerben",
  "license": "CC0-1.0",
  "dependencies": {
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-1": "^6.5.0",
    "babelify": "^7.2.0",
    "watchify": "^3.7.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
