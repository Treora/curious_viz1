{
  "name": "curious_viz",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "browserify src/setglobals.js -o main.js",
    "watch": "watchify src/setglobals.js -o main.js -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gerben",
  "license": "CC0-1.0",
  "dependencies": {
    "gaussian": "^1.1.0",
    "json-stable-stringify": "^1.0.1",
    "linspace": "^1.0.0",
    "lodash": "^4.13.1",
    "random-seed": "^0.3.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-1": "^6.5.0",
    "babelify": "^7.2.0",
    "watchify": "^3.7.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
